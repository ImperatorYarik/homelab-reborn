- name: Update apt cache
  apt:
    update_cache: yes
    cache_valid_time: 3600

- name: Install common software packages
  apt:
    name:
      - git
      - curl
      - wget
      - htop
      - net-tools
    state: present